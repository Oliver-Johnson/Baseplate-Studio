# Anything not meant for the repo lives in one place, so this file never has to
# name it. Put working notes, scratch scripts and experiments in there.
Local Workspace/

# Build and test output
node_modules/
out/
test-results/
playwright-report/
*.stl
*.3mf
*.log
__pycache__/
*.pyc

# Agent worktrees. These are checkouts of this same repo, so committing them
# nests a git repository inside itself and publishes work in progress.
.claude/

# Editor and OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini
