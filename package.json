{
  "name": "drawerforge",
  "version": "1.0.0",
  "private": true,
  "description": "Gridfinity baseplates and bins, built to the drawer you actually have.",
  "license": "MIT",
  "scripts": {
    "build": "node build.js",
    "test": "node build.js --check && node test/bin-audit.js && node test/plate-audit.js && node test/fit-check.js && node test/stack-check.js && node test/hash-roundtrip.js && node test/seo-check.js",
    "test:ui": "playwright test",
    "test:all": "npm test && npm run test:ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
